# Invasion of Lindisfarne!

Protect Lindisfarne from the viking invaders!

Space Invaders clone in Raylib.


## Web build note:

Command:
```shell
make default PLATFORM=PLATFORM_WEB -B
```

Then checkout `github-pages` branch and copy the contents of `web/` to the root
dir. Push updates to the branch.
